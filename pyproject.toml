[build-system]
requires = ["setuptools>=69", "wheel"]
build-backend = "setuptools.build_meta"

[project]
name = "game-ui-framework"
version = "1.0.0a24"
description = "Local-first, privacy-separated game UI production framework with authenticated Hosts and Git change management"
readme = "README.md"
requires-python = ">=3.10"
license = {text = "MIT"}
authors = [{name = "GUIF Contributors"}]
dependencies = []

[project.optional-dependencies]
dev = ["pytest>=8.0", "Pillow>=10.0"]
image = ["Pillow>=10.0"]

[project.scripts]
guif = "guif.cli:main"
guif-ops = "guif.ops_cli:main"
guif-gateway = "guif.gateway_cli:main"
guif-ledger = "guif.ledger_cli:main"

[tool.setuptools.packages.find]
include = ["guif*"]

[tool.pytest.ini_options]
testpaths = ["tests"]
